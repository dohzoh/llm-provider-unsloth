name: CI/CD

on:
  push:
    branches: [main]
    tags: ['pi@*', 'opencode@*']
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm turbo build

      - name: Lint
        run: pnpm turbo lint

      - name: Test
        run: pnpm turbo test

  publish-pi:
    name: Publish @dohzoh/pi-unsloth
    runs-on: ubuntu-latest
    needs: ci
    if: startsWith(github.ref, 'refs/tags/pi@')
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/pi@}" >> $GITHUB_OUTPUT

      - name: Update package version
        run: pnpm --filter @dohzoh/pi-unsloth version ${{ steps.version.outputs.VERSION }} --no-git-check

      - name: Publish to GitHub Packages
        run: |
          echo "//npm.pkg.github.com/:_authToken=$NODE_AUTH_TOKEN" >> ~/.npmrc
          pnpm --filter @dohzoh/pi-unsloth publish --no-git-check
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-opencode:
    name: Publish @dohzoh/opencode-unsloth
    runs-on: ubuntu-latest
    needs: ci
    if: startsWith(github.ref, 'refs/tags/opencode@')
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install

      - name: Get version from tag
        id: version
        run: echo "VERSION=${GITHUB_REF#refs/tags/opencode@}" >> $GITHUB_OUTPUT

      - name: Update package version
        run: pnpm --filter @dohzoh/opencode-unsloth version ${{ steps.version.outputs.VERSION }} --no-git-check

      - name: Publish to GitHub Packages
        run: |
          echo "//npm.pkg.github.com/:_authToken=$NODE_AUTH_TOKEN" >> ~/.npmrc
          pnpm --filter @dohzoh/opencode-unsloth publish --no-git-check
        env:
          NODE_AUTH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
