name: Auto Publish Pi

on:
  push:
    branches: [main]
    paths:
      - 'packages/pi/**'

permissions:
  contents: write
  packages: write

jobs:
  publish:
    name: Publish @dohzoh/pi-unsloth
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://npm.pkg.github.com'
          scope: '@dohzoh'

      - name: Install dependencies
        run: npm install

      - name: Get version
        run: echo "VERSION=$(node -p "require('./packages/pi/package.json').version")" >> $GITHUB_OUTPUT
        id: version

      - name: Publish to GitHub Packages
        run: |
          cd packages/pi
          npm publish --access public --no-git-check

      - name: Create Git Tag
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag -a pi@${{ steps.version.outputs.VERSION }} -m "pi provider ${{ steps.version.outputs.VERSION }}"
          git push origin pi@${{ steps.version.outputs.VERSION }}
